/**
 * These credentials can also be added as environment variables with same key names
 * instead of putting here.
 */
module.exports = {
	// App ID generated while registering the app with Microsoft.
	"microsoftAppId": "",

	// App password generated while registering the app with Microsoft.
	"microsoftAppPassword": "",

	// Your own shared secret used for using proactive message endpoint.
	"sharedSecret": ""
};